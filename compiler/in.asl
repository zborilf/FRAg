go.

+!mujplanicek(A,B):check(A)&A<43&guard(X)<- -ahoj(kamarade);+kamarad(pepa);.writeln('sakra');?value(V);!goNext.

!go(X).

!count(P).
!count(A,B,C).

david(a).

jirka(ahoj_pepo).
jirka(ahoj_pepo,X,_pivo,23.114,pivko).


                                                                  
